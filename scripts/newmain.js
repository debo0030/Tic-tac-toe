// When 'Start Game' button is clicked checks for players. If null then class getUserInfo function.
// calls generateGameBoard function.
$('#startGame').on('click', function() {
  module.initGame();
});
